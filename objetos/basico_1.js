let num = [10, 20, 30]; //array
let product = { //objeto - trabalha como pares [chave: valor]. As chaves devem ser únicas dentro de um objeto.
    name: "Caneta",
    type: "Esferográfica"


};

console.log(num[2]);
console.log(product["type"]);
console.log(product.name, product.type); //forma mais utilizada